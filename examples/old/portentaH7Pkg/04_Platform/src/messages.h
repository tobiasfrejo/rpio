#ifndef MESSAGES_H
#define MESSAGES

// Define the custom messages that will be used with the RaPClient

struct Flag {
    bool value;
    // Add more fields as needed
};

struct RobotPose {
    char value;
    // Add more fields as needed
};


#endif // MESSAGE_H